import {StyleSheet} from 'react-native';

const VerifyCodeStyle = StyleSheet.create({
  main: {
    flex: 1,
    height: '100%',
    width: '100%',
  },
  lowBody: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 40,
    alignItems: 'center',
  },
});

export default VerifyCodeStyle;
