import {StyleSheet} from 'react-native';

const SignInStyle = StyleSheet.create({
  signInMain: {
    flex: 1,
    height: '100%',
    width: '100%',
  },
  lowBody: {
    marginTop: 40,
    alignItems: 'center',
  },
});
export default SignInStyle;
