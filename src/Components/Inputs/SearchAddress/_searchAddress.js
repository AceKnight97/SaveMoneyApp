import {
  StyleSheet,
} from 'react-native';


const SearchAddressStyle = SearchAddressStyleheet.create({
  container: {
    display: 'flex',
    flexDirection: 'column',
    marginTop: 16,
  },
  Page: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
  },
});

export default SearchAddressStyle;
